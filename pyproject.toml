[project]
name = "testnovation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mido>=1.3.3",
    "numpy>=2.4.4",
    "python-rtmidi>=1.5.8",
    "sounddevice>=0.5.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
